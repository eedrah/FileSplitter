# FileSplitter

Splits files where a `var ` occurs. The filenames are taken from the `var` name and the extension of the original file.

`npm install` to before running.

`cd` into the directory of your file and run with `node file-splitter.js myFile.jsx`.

Text before the first `var ` will be outputted in a file with no name and given extension.
